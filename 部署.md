# 部署（ECS + HTTPS）

备案已通过后的部署说明。日常开发仍看 [README.md](README.md)；**当前要不要开 80/443、怎么上线，以本文为准。** README 里「备案前不要开端口」的句子尚未改，那是备案审核期间的记录。

本文不写密码、微信 Secret、员工口令。生产 `.env` 只放服务器，不进 Git、不进对话。

## 1. 现在做到哪

目标地址：

| 用途         | 地址                                             |
| ------------ | ------------------------------------------------ |
| 游客 H5      | `https://qucamp.cn`（`www` 以后 301 到根域） |
| 员工后台     | `https://admin.qucamp.cn`                      |
| 微信授权回调 | `https://qucamp.cn/api/auth/wechat/callback`   |
| 健康检查     | `https://qucamp.cn/health`                     |

多营地仍是路径前缀 `/{campSlug}`（如 `/luhe`），不按营地域名拆 Cookie。

### 已完成

- 2C4G Ubuntu 24.04，能 SSH
- Docker CE + Compose 已装，`systemctl enable --now docker`
- 域名 `qucamp.cn`；A 记录 `@` / `www` / `admin` 已指到该机公网 IPv4（`124.223.176.112`）
- ICP 备案已通过
- 本机可用同一套 Compose 预演「静态页 + 反代 `/api`」：`sh deploy/web/build-static.sh` 后 `docker compose --profile web up -d --build`，H5 在 8080、后台在 8081
- 容器启动时 `alembic upgrade head` 已写在 [backend/entrypoint.sh](backend/entrypoint.sh)，生产可沿用迁移这一步

### 站点还没上，现有仓库也还不是生产形态

本机 `docker compose --profile web up` **不能原样打到公网**。原因：

- [compose.yaml](compose.yaml) 把 Postgres `5433`、FastAPI `8000` 映射到宿主机；backend 还挂了 `./backend:/app`
- entrypoint 是 `uvicorn ... --reload`（开发热重载）
- [deploy/nginx/default.conf](deploy/nginx/default.conf) 只在容器内听 `80`（H5）和 `81`（后台），`server_name _`，没有证书、没有 HTTPS、没有 `www` → 根域
- [deploy/web/Dockerfile](deploy/web/Dockerfile) 只拷贝本机已经 `npm run build` 的 `frontend/dist`、`admin/dist`，镜像里不装 Node（这点生产要对：前端继续本机构建）
- 微信 `WECHAT_APP_ID` / `WECHAT_APP_SECRET` 都空则走 mock，全员同一个 `mock-local-openid`，不能给真实客人下单
- 第 23 步备份脚本还不存在；[backup/](backup/) 只是目录约定，dump 被 gitignore

对外最终只应有：**22**（尽量限来源 IP）、**80**、**443**。不要对公网开 5433、8000。

```text
访客浏览器
  https://qucamp.cn、www     → 主机 443 → Nginx 按域名给出 H5（www 再跳到根域）
  https://admin.qucamp.cn    → 主机 443 → Nginx 按域名给出员工后台
  http://上述域名            → 主机 80  → 301 到对应的 https
Nginx 容器内
  /api、/health → Compose 服务名 backend:8000（不要用公网 IP）
backend
  连 postgres:5432（不要映射到 0.0.0.0）
```

本机 8080 / 8081 只是预览口：容器内 H5 听 80、后台听 81，不挂证书。生产访客只走域名的 80 和 443，靠 `server_name` 区分两个站，不再靠 8081。

## 2. 上线前配置清单

按这项做完再对外提供页面。控制台操作与服务器配置分开；**先补 HTTPS 相关配置，再把 80/443 给公网用户。**

### 2.1 腾讯云安全组

| 端口        | 公网                                         |
| ----------- | -------------------------------------------- |
| 22          | 开，来源尽量限家/办公室 IP                   |
| 80          | 开，来源 IPv4 `0.0.0.0/0`（HTTP 跳 HTTPS） |
| 443         | 开，来源 IPv4 `0.0.0.0/0`。不要只开 IPv6 `::/0` |
| 5433        | 关                                           |
| 8000        | 关                                           |
| 8080 / 8081 | 关（那是本机预览，不是生产入口）             |

安全组开了 ≠ Compose 可以继续把 5433/8000 绑到 `0.0.0.0`。云上这两项不要发布到公网网卡。

### 2.2 服务器时区

```bash
timedatectl set-timezone Asia/Shanghai
timedatectl
```

Compose 里 postgres / backend 的 `TZ`、`PGTZ` 用 `Asia/Shanghai`（见 [.env.example](.env.example)）。系统时区和容器时区都要上海，排期时间才和后台日期选择器一致。

### 2.3 生产 `.env`（服务器单独一份）

不要把本机 `.env` 拷上去。本机是 `http://127.0.0.1:5173` 和 `SESSION_COOKIE_SECURE=false`。生产至少：

```bash
TZ=Asia/Shanghai

# 强密码，与本机不同
POSTGRES_PASSWORD=
ADMIN_USERNAME=
ADMIN_PASSWORD=

H5_ORIGIN=https://qucamp.cn
ADMIN_ORIGIN=https://admin.qucamp.cn
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_NAME=ab_session
ADMIN_SESSION_COOKIE_NAME=ab_admin_session
SESSION_TTL_SECONDS=604800

# HTTPS 通了再填；都空则仍是 mock，全员同一 openid
WECHAT_APP_ID=
WECHAT_APP_SECRET=
WECHAT_OAUTH_REDIRECT_URI=https://qucamp.cn/api/auth/wechat/callback
WECHAT_OAUTH_STATE_SECRET=

WECHAT_TPL_BOOKING_SUCCESS=
WECHAT_TPL_BOOKING_CANCEL=
WECHAT_TPL_BOOKING_REMINDER=
```

Cookie 是否带 `Secure`、登录 302、CORS 允许源，都只看这些变量（[backend/app/core/config.py](backend/app/core/config.py)、[backend/app/core/middleware.py](backend/app/core/middleware.py)）。`H5_ORIGIN` 写错，微信 callback 会把人送回错误的源。

生产 `DATABASE_URL` 由 Compose 覆盖为容器内 `postgres:5432`，不必在 `.env` 里写公网地址。

`ADMIN_PASSWORD` 若含 `#` 必须加引号。改完 `.env` 后要重建 backend 容器，再跑 `seed_staff.py`，否则员工仍是旧哈希。

大陆拉 Docker Hub 失败时，在服务器 `.env` 取消注释镜像加速（与 [.env.example](.env.example) 相同变量），不要在 2C 机器上改用本机构建以外的方式编前端。

### 2.4 腾讯云免费证书

这次用腾讯云免费 DV 证书（TrustAsia），在 [SSL 证书控制台](https://console.cloud.tencent.com/ssl) 申请。域名已经托管在腾讯云解析，验证选**自动 DNS**。站点还没起来也能签发，不依赖 80 端口放验证文件。

免费证有效期 **90 天**，一张只绑一个域名，不支持 `*.qucamp.cn`，也不会自动写进 Docker。到期前在控制台重新申请，覆盖服务器上的文件，再重载 Nginx。

需要两张：

| 绑定域名 | 覆盖 | 给谁用 |
| --- | --- | --- |
| `qucamp.cn` | 签发后附带 `www.qucamp.cn` | 游客 H5 |
| `admin.qucamp.cn` | 只有这一个名字 | 员工后台 |

`admin` 写不进第一张。不要绑公网 IP。

当前 [deploy/nginx/default.conf](deploy/nginx/default.conf) 还没有 443 和 `server_name`。证书可以先申请、先放到服务器；Nginx 配置和 Compose 端口要另外改完，浏览器才能打开 HTTPS。生产 Compose 仍不要发布 5433/8000，不要挂 `./backend:/app`，不要 `--reload`。

#### 申请

账号需已实名。控制台搜索「SSL 证书」→「我的证书」→「申请免费证书」。

第一张：

1. 绑定域名填 `qucamp.cn`（不要加 `https://`，不要加路径）。
2. 域名验证选**自动 DNS 验证**。域名已在云解析里时，控制台会自己加 TXT，不必手抄记录值。
3. 密钥算法选 **RSA**（Nginx 这份配置按 RSA 私钥来挂）。
4. 提交后等状态变成**已签发**。验证通过后通常几分钟，官方说明最长约 24 小时。

第二张同样步骤，绑定域名改成 `admin.qucamp.cn`。两张都显示已签发再下载。

自动 DNS 不可用时，改手动 DNS：按页面给出的主机记录和记录值，在云解析里加一条 TXT，等验证通过。不要改已有的 A 记录。

#### 下载

每张证书点「下载」→ 服务器类型选 **Nginx** → 解压。

用这两个文件：

- `*_bundle.crt`（或文件名里带 `bundle` 的证书；含中间证书，Nginx 要这份）
- `*.key`（私钥）

`.csr` 不用拷上服务器。解压包里若同时有 `.pem` 和 `_bundle.crt`，Nginx 的 `ssl_certificate` 指向 **bundle** 那份。

本机检查名字（把路径换成解压目录）：

```bash
openssl x509 -in qucamp.cn_bundle.crt -noout -subject -ext subjectAltName -dates
openssl x509 -in admin.qucamp.cn_bundle.crt -noout -subject -ext subjectAltName -dates
```

第一张的名字里应有 `qucamp.cn` 和 `www.qucamp.cn`。第二张应是 `admin.qucamp.cn`。`notAfter` 大约是签发日起 90 天。

#### 放到 ECS

证书放在仓库外面，不进 Git、不打进镜像：

```text
/opt/activity-booking/certs/qucamp.cn_bundle.crt
/opt/activity-booking/certs/qucamp.cn.key
/opt/activity-booking/certs/admin.qucamp.cn_bundle.crt
/opt/activity-booking/certs/admin.qucamp.cn.key
```

在服务器上：

```bash
sudo mkdir -p /opt/activity-booking/certs
sudo chmod 700 /opt/activity-booking/certs
```

在自己电脑上把四个文件传上去（文件名按解压结果改）：

```bash
scp qucamp.cn_bundle.crt qucamp.cn.key \
  admin.qucamp.cn_bundle.crt admin.qucamp.cn.key \
  <用户>@124.223.176.112:/tmp/
```

再在服务器上移入目录并收紧私钥：

```bash
sudo mv /tmp/qucamp.cn_bundle.crt /tmp/qucamp.cn.key \
  /tmp/admin.qucamp.cn_bundle.crt /tmp/admin.qucamp.cn.key \
  /opt/activity-booking/certs/
sudo chmod 644 /opt/activity-booking/certs/*.crt
sudo chmod 600 /opt/activity-booking/certs/*.key
sudo chown root:root /opt/activity-booking/certs/*
```

传完后删掉本机解压包和 `/tmp` 里的副本。私钥不要发到聊天、不要提交。

Nginx 跑在容器里，只放在宿主机 `/etc/nginx` 它也读不到。上线改 [compose.yaml](compose.yaml) 时把该目录只读挂进去，例如 `/opt/activity-booking/certs:/etc/nginx/certs:ro`。容器内路径固定成：

```text
/etc/nginx/certs/qucamp.cn_bundle.crt
/etc/nginx/certs/qucamp.cn.key
/etc/nginx/certs/admin.qucamp.cn_bundle.crt
/etc/nginx/certs/admin.qucamp.cn.key
```

#### Nginx 怎么用这两张证

不要 `apt install nginx`。生产在现有 Compose 的 Nginx 上改，宿主机发布 `80:80` 和 `443:443`（本机预览仍可用 8080/8081，那套不挂这些证书）。

容器里同一份配置大致是：

- `qucamp.cn`、`www.qucamp.cn` 听 `443 ssl`，证书用第一张。`www` 的 443 只做 `301` 到 `https://qucamp.cn`。
- `admin.qucamp.cn` 听 `443 ssl`，证书用第二张。
- 这三个名字的 `80` 都 `301` 到 https。`www` 直接跳到 `https://qucamp.cn`。
- H5 与 `/api` 仍同域；后台独立 `server`，Cookie 分源。`/api`、`/health` 继续 `proxy_pass http://backend:8000`。

对应指令：

```nginx
ssl_certificate     /etc/nginx/certs/qucamp.cn_bundle.crt;
ssl_certificate_key /etc/nginx/certs/qucamp.cn.key;
ssl_protocols       TLSv1.2 TLSv1.3;
```

后台那个 `server` 换成 `admin.qucamp.cn` 的 crt 和 key。

改完后在容器里检查再重载（不要在宿主机跑 `systemctl reload nginx`）：

```bash
docker compose exec nginx nginx -t
docker compose exec nginx nginx -s reload
```

#### 换证（每 90 天）

提前约两周：控制台对同一域名再申请一张免费证，状态变为已签发后按上面的文件名覆盖 `/opt/activity-booking/certs/`，然后再次 `nginx -t` 和 `nginx -s reload`。覆盖期间不要改文件名，配置里的路径就不用动。

一个账号最多约 50 张免费证。旧证吊销或到期后大约 24 小时额度才释放。不要等到过期当天才换，微信里会直接报不安全。

### 2.5 公众平台（HTTPS 通了再填）

网页授权：

- 授权回调域名只填 `qucamp.cn`（不要 `https://`，不要路径，不要 `www` 除非你也把它配进去）
- `redirect_uri` 必须是 `https://qucamp.cn/api/auth/wechat/callback`，与 `.env` 的 `WECHAT_OAUTH_REDIRECT_URI` 一致
- 需要已认证**服务号**（`snsapi_base`）

模板消息（可后于登录）：

- 对应模板 ID 写入 `.env`
- API **IP 白名单**填这台 ECS 的出口 IPv4（通常就是公网 IP）
- 用户须已关注该服务号

AppID / Secret 都非空才走真授权；模板 ID 再缺则只写 `notification_logs`、不调微信。不要把 mock 当成正式营业。

### 2.6 前端构建方式（前置习惯）

2 核 ECS **不要**现场 `npm ci && npm run build`。本机：

```bash
sh deploy/web/build-static.sh
```

再把含 `frontend/dist`、`admin/dist` 的 Nginx 镜像打出来拷上云（或在本机构建后 `docker save` / 私有仓库拉取）。[deploy/web/Dockerfile](deploy/web/Dockerfile) 假定 dist 已经存在。

## 3. 流程逐步解析

现有脚本能做的，和还缺的，分开写。顺序不要跳。

```text
本机构建 dist
  → 打 backend / nginx 镜像（postgres 用官方或国内镜像）
  → 镜像与生产 .env 放到 ECS
  → 先起 postgres + backend（自动 alembic）
  → seed 目录与员工
  → Nginx 以 80/443 对外（配置补齐后）
  → 验收 HTTPS
  → 再填微信并真授权联调
```

### 3.1 本机构建静态资源

```bash
sh deploy/web/build-static.sh
```

得到 `frontend/dist`、`admin/dist`。这是打进 Nginx 镜像的快照；之后改 Vue 不会自动出现在线上，要重新 build 再换 nginx 镜像。

### 3.2 镜像上 ECS

本机可先：

```bash
docker compose build
docker compose --profile web build
```

不要在云上装 Node、不要 `apt install nginx`。站点 Nginx 走 Compose 里的镜像，和本机 profile `web` 同一条路，只是生产要换成 80/443 + 证书，而不是宿主机 8080/8081。

拷镜像或拉仓库都可以，但 **`.env` 用服务器那份**。代码同步时注意不要覆盖生产 `.env`。

### 3.3 Postgres + backend

生产应只起数据库和 API，**不**把 5433/8000 绑到 `0.0.0.0`。容器起来后 entrypoint 会 `alembic upgrade head`，然后 uvicorn。

验收（在服务器上打容器网或经 Nginx，不要对公网暴露 8000）：

```bash
# 示例：容器内
docker compose exec backend python -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:8000/health').read())"
```

应得到 `{"status":"ok"}`。改表永远靠 Alembic + **重启 backend**，不要 `docker compose down -v` 当刷新结构。

### 3.4 种子数据

空库需要目录和员工账号：

```bash
docker compose exec backend python scripts/seed_catalog.py
docker compose exec backend python scripts/seed_staff.py
```

`seed_catalog.py` 可重复执行；已有预约的场次不会被删。`seed_staff.py` 按 `.env` 的 `ADMIN_USERNAME` / `ADMIN_PASSWORD` 写入绑定 `luhe` 的员工，缺密码则退出。无公开注册。

生产若已有真实预约，不要为了「再跑一遍演示数据」而清库。

### 3.5 Nginx 对外

证书按 [2.4](#24-腾讯云免费证书) 放在 `/opt/activity-booking/certs/`，配置补齐 `server_name` 与 443 之后：

- 访客：`https://qucamp.cn/luhe`
- 员工：`https://admin.qucamp.cn`
- `/api` 与 `/health` 反代 `http://backend:8000`
- 游客 Cookie `ab_session` 写在 `qucamp.cn`；员工 `ab_admin_session` 写在 `admin.qucamp.cn`

本机预览命令（不要当成生产启动）：

```bash
sh deploy/web/build-static.sh
docker compose --profile web up -d --build
curl -s http://127.0.0.1:8080/health
curl -sI http://127.0.0.1:8080/luhe
```

那时要把本机 `.env` 的 origin 临时改成 8080/8081 才能测登录 Cookie。生产则固定为 https 域名。

### 3.6 真微信

HTTPS 与授权域名都通了，再把 AppID、Secret、`WECHAT_OAUTH_REDIRECT_URI`、state secret 写入服务器 `.env`，重建 backend。在微信里打开 `https://qucamp.cn/luhe` 走 `GET /api/auth/wechat/start?camp=luhe`，callback 应 302 回 `https://qucamp.cn/luhe` 且带 HttpOnly Cookie（`Secure`）。

模板消息可同一次或稍后：模板 ID + IP 白名单。

## 4. 不要做

- `docker compose down -v`（删 named volume `postgres_data`，库不可恢复）
- 在 2C ECS 上 `npm run build` / `npm ci`
- 把本机 `.env` 原样拷到服务器
- 把 `.env`、密码、Secret、证书私钥（`.key`）提交进 Git 或贴进文档
- 对公网映射 5433、8000
- 用 mock 登录给营地客人下单（全员同一 openid，预约会串）
- 备案前那套「只开 8080/8081 + 公网 IP」当正式入口（现已备案，走域名 HTTPS）
- 微信授权域名填 `https://`、路径、或 `127.0.0.1`
- 以为改了 Vue 源码线上 443 会自动更新（要重新 build 静态并换 nginx 镜像）

## 5. 验收

HTTPS 起来后：

1. `curl -sI https://qucamp.cn/health` → 200，body 含 `"ok"`
2. `https://qucamp.cn/` 进 `/luhe`；未知 slug 为 H5 404
3. `https://admin.qucamp.cn/` 用员工账号能登录；401 回 `/login`，不走微信
4. 浏览器里 `ab_session` / `ab_admin_session` 为 HttpOnly，且仅在 HTTPS 下种上（`SESSION_COOKIE_SECURE=true`）
5. H5 与 `/api` 同域（都是 `qucamp.cn`），登录不必跨域带第三方面包
6. 微信内打开活动页能完成授权（凭据配齐后）；「我的预约」只列当前 URL 营地
7. 后台改价不影响已下单 `total_price`；关场次不删已有预约

## 6. 上线之后（不算这次前置）

- 第 23 步：数据库自动备份（往 [backup/](backup/) 写 dump，文件已 gitignore）
- SSH 密钥登录、禁用密码登录
- 生产关掉 `--reload`、去掉源码 bind mount（与 2.4 一起改 Compose）

这些不影响「先把 HTTPS 站点立住」，但数据库没有自动备份前，不要把这台机当成唯一数据副本。
